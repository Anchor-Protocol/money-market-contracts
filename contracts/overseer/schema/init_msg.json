{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InitMsg",
  "type": "object",
  "required": [
    "buffer_distribution_rate",
    "distribution_threshold",
    "epoch_period",
    "liquidation_model",
    "market_contract",
    "oracle_contract",
    "owner_addr",
    "stable_denom",
    "target_deposit_rate"
  ],
  "properties": {
    "buffer_distribution_rate": {
      "description": "Ratio to be distributed from the interest buffer",
      "allOf": [
        {
          "$ref": "#/definitions/Decimal"
        }
      ]
    },
    "distribution_threshold": {
      "description": "Distribute interest buffer to market contract, when deposit_rate < distribution_threshold",
      "allOf": [
        {
          "$ref": "#/definitions/Decimal"
        }
      ]
    },
    "epoch_period": {
      "title": "of blocks per epoch period",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "liquidation_model": {
      "description": "Liquidation model contract address to compute liqudation amount",
      "allOf": [
        {
          "$ref": "#/definitions/HumanAddr"
        }
      ]
    },
    "market_contract": {
      "description": "Market contract address to receive missing interest buffer",
      "allOf": [
        {
          "$ref": "#/definitions/HumanAddr"
        }
      ]
    },
    "oracle_contract": {
      "description": "Oracle contract address for collateral tokens",
      "allOf": [
        {
          "$ref": "#/definitions/HumanAddr"
        }
      ]
    },
    "owner_addr": {
      "description": "Initial owner address",
      "allOf": [
        {
          "$ref": "#/definitions/HumanAddr"
        }
      ]
    },
    "stable_denom": {
      "description": "The base denomination used when fetching oracle price, reward distribution, and borrow",
      "type": "string"
    },
    "target_deposit_rate": {
      "description": "Target deposit rate. When current deposit rate is bigger than this, Custody contracts send rewards to interest buffer",
      "allOf": [
        {
          "$ref": "#/definitions/Decimal"
        }
      ]
    }
  },
  "definitions": {
    "Decimal": {
      "description": "A fixed-point decimal value with 18 fractional digits, i.e. Decimal(1_000_000_000_000_000_000) == 1.0\n\nThe greatest possible value that can be represented is 340282366920938463463.374607431768211455 (which is (2^128 - 1) / 10^18)",
      "type": "string"
    },
    "HumanAddr": {
      "type": "string"
    }
  }
}
