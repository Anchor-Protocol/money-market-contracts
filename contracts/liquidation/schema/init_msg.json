{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InitMsg",
  "type": "object",
  "required": [
    "liquidation_threshold",
    "min_liquidation",
    "owner",
    "safe_ratio"
  ],
  "properties": {
    "liquidation_threshold": {
      "description": "Liquidation threshold amount in stable denom. When the current collaterals value is smaller than the threshold, all callterals will be liquidated",
      "allOf": [
        {
          "$ref": "#/definitions/Uint256"
        }
      ]
    },
    "min_liquidation": {
      "description": "Minimum liquidation amount. If a liquidation amount is below than this value, exclude the asset from the liquidation process",
      "allOf": [
        {
          "$ref": "#/definitions/Uint256"
        }
      ]
    },
    "owner": {
      "$ref": "#/definitions/HumanAddr"
    },
    "safe_ratio": {
      "description": "borrow_amount / borrow_limit must always be bigger than safe_ratio.",
      "allOf": [
        {
          "$ref": "#/definitions/Decimal256"
        }
      ]
    }
  },
  "definitions": {
    "Decimal256": {
      "description": "A fixed-point decimal value with 18 fractional digits, i.e. Decimal256(1_000_000_000_000_000_000) == 1.0 The greatest possible value that can be represented is 115792089237316195423570985008687907853269984665640564039457.584007913129639935 (which is (2^128 - 1) / 10^18)",
      "type": "string"
    },
    "HumanAddr": {
      "type": "string"
    },
    "Uint256": {
      "type": "string"
    }
  }
}
